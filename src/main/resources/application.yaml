server:
  port: 9000

spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://175.24.176.171/db_older?useSSL=false&useUnicode=true&characterEncoding=utf-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: root
    password: 807811602Wang

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  logging:
    level:
      com.yjy.beadhouse.dao: debug